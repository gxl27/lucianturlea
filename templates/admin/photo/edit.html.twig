{% extends 'base.html.twig' %}

{% block title %}Edit Photo{% endblock %}

{% block body %}
    <h1>Edit Photo</h1>

    {{ include('admin/photo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('photo_index') }}">back to list</a>

    {{ include('admin/photo/_delete_form.html.twig') }}
{% endblock %}
